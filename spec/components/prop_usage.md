# Using `props`

## Found Within Components
`props` are found within  a component's `prop` attribute and are a series of `PROPNAME PROPTYPE` pairs seperated by commas.

## Full example
Here is a full example of a component which makes use of a `prop` with the name being `text`:

```html
<button props="text string">{text}</button>
```
